import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'recilica-appp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
